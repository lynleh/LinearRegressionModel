% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Linear_Regression_Model.R
\name{linear_regression_model}
\alias{linear_regression_model}
\title{Fit a basic linear regression: y ~ x}
\usage{
linear_regression_model(x, y, na.rm = TRUE, plot = FALSE)
}
\arguments{
\item{x}{A numeric vector of predictor values.}

\item{y}{A numeric vector of outcome values, same length as x.}

\item{na.rm}{Logical; if TRUE, rows with NA in x or y are removed before fitting.}

\item{plot}{Logical; if TRUE, a plot of data and regression line is shown.}
}
\value{
A list with components: coefficients (intercept, slope), fitted, residuals,
sigma (residual standard error), R2 (coefficient of determination),
n (number of observations), call (the function call).
}
\description{
This function uses a step by step process to fit a straight line to data with one predictor (x) and one outcome (y).
It computes the intercept and slope using ordinary least squares (OLS).
It also returns fitted values, residuals, and basic summary information.
Optionally, it produces a plot showing the data points and fitted regression line.
}
\examples{
x = 1:10
y = 2 + 0.5 * x + rnorm(10, 0, 0.1)
fit = linear_regression_model(x, y, plot = TRUE)
fit$coefficients
}
